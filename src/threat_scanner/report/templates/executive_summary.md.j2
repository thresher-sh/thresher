# Executive Security Summary

| Field | Value |
|-------|-------|
| **Scan Date** | {{ scan_date }} |
| **Target Repository** | {{ repo_url }} |
| **Dependency Depth** | {{ depth }} |
| **AI Analysis** | {{ "Disabled (--skip-ai)" if skip_ai else "Enabled" }} |

---

## Overall Risk Assessment

# {{ risk_assessment }}

{% if risk_assessment == "DO NOT USE" %}
**This project has P0 or Critical findings that indicate serious security risk. Do not use without thorough review and remediation.**
{% elif risk_assessment == "CAUTION" %}
**This project has High-severity findings. Use with caution and ensure mitigations are in place before adoption.**
{% else %}
**No high-severity findings detected. This project appears acceptable for use based on automated analysis.**
{% endif %}

### Decision Criteria

- Any **P0** or **Critical** finding = **DO NOT USE**
- **High** findings only = **CAUTION**
- **Medium** and below only = **GO**

---

## Finding Counts by Priority

| Priority | Count |
|----------|-------|
| **P0 (Emergency)** | {{ priority_counts.get("P0", 0) }} |
| **Critical** | {{ priority_counts.get("critical", 0) }} |
| **High** | {{ priority_counts.get("high", 0) }} |
| **Medium** | {{ priority_counts.get("medium", 0) }} |
| **Low** | {{ priority_counts.get("low", 0) }} |
| **Total** | {{ total_findings }} |

---

## Top 10 Findings

| # | Priority | Title | Source Tool | CVE ID | CVSS |
|---|----------|-------|-------------|--------|------|
{% for finding in top_findings %}
| {{ loop.index }} | {{ finding.get("composite_priority", "low") | upper }} | {{ finding.get("title", "Unknown") }} | {{ finding.get("source_tool", "N/A") }} | {{ finding.get("cve_id", "N/A") }} | {{ finding.get("cvss_score", "N/A") }} |
{% endfor %}
{% if not top_findings %}
| - | - | No findings detected | - | - | - |
{% endif %}

---

## SBOM Summary

| Metric | Value |
|--------|-------|
| **Total Packages** | {{ total_packages }} |
| **Ecosystems** | {{ ecosystems | join(", ") if ecosystems else "N/A" }} |

---

*Report generated by Project Threat Scanner on {{ scan_date }}.*
