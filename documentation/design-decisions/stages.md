# Stages

Each stage is done inside the pipeline.

Most of them are described outside of this... some are inline here... maybe (if I ever add them)

Stages should look for specific things on the disk, and do something, then give an output.

Agents look at folders and run their loops then output summaries and findings, for example.

Stages should have names and a goal, should not be blind function calling.